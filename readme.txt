Some example program may crash due to a bug in MLE controls
when their width is less than a few pixels.
IBM released a fixed version of PMCTLS.DLL but I do not know
if it is publically available.
If you need it just drop me a note (it is a 500 MB file).
